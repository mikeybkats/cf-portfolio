page('/', allPagesController.reveal);
page('/about', aboutController.reveal);
page('/work', workController.reveal);
page('/projects', postsBlogController.reveal);
page('/contact', contactController.reveal);

page();
